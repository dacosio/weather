import React from "react";
import styles from "./forecast.module.css";

const Forecast = ({ forecastData, address }) => {
  return (
    <div className={styles.container}>
      wearher 
    </div>
  );
};

export default Forecast;
